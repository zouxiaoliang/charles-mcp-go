# 实时与历史分析共用捕获和记录模型

上游普通抓包与逆向分析使用不同的记录 ID 和存储模型，跨工具传递证据容易混淆。Go 版将两者统一为 Capture 与 Entry，Live Session 仅表示观察过程和读取进度；代价是放弃旧参数及数据文件的内部结构兼容，收益是查询、解码、重放和分析可以直接引用同一记录。

同一请求的响应更新沿用 Entry ID，Charles 清空会话时新建身份代次。持久化捕获不随观察会话过期而删除，历史清理由显式操作完成。
